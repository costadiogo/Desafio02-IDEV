let totalGeral = 0;
let fatorMult = 7;
let indice = 1;

totalGeral += fatorMult * indice;
console.log(`${fatorMult} x ${indice} : ${fatorMult * indice}`);
indice++;

totalGeral += fatorMult * indice;
console.log(`${fatorMult} x ${indice} : ${fatorMult * indice}`);
indice++;

totalGeral += fatorMult * indice;
console.log(`${fatorMult} x ${indice} : ${fatorMult * indice}`);
indice++;

totalGeral += fatorMult * indice;
console.log(`${fatorMult} x ${indice} : ${fatorMult * indice}`);
indice++;

totalGeral += fatorMult * indice;
console.log(`${fatorMult} x ${indice} : ${fatorMult * indice}`);
indice++;

totalGeral += fatorMult * indice;
console.log(`${fatorMult} x ${indice} : ${fatorMult * indice}`);
indice++;

totalGeral += fatorMult * indice;
console.log(`${fatorMult} x ${indice} : ${fatorMult * indice}`);
indice++;

totalGeral += fatorMult * indice;
console.log(`${fatorMult} x ${indice} : ${fatorMult * indice}`);
indice++;

totalGeral += fatorMult * indice;
console.log(`${fatorMult} x ${indice} : ${fatorMult * indice}`);
indice++;

totalGeral += fatorMult * indice;
console.log(`${fatorMult} x ${indice} : ${fatorMult * indice}`);
indice++;

console.log(" Total = " + totalGeral);